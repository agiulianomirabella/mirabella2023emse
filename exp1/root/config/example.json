{
    "restest": {
        "properties": "GitHub/github_getUserRepos",
        "results":    "githubGetUserRepos"
    },

    "types": {
        "visibility":   "enum",
        "affiliation":  "text",
        "type":         "enum",
        "sort":         "enum",
        "direction":    "enum",
        "per_page":     "number",
        "page":         "number",
        "since":        "text",
        "before":       "text"
    },

    "api_keys": [
        
    ],

    "class_weight":  "balanced",
    "sample_method": "smote"
    
}
